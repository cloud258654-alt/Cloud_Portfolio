# Database Design

## IndexedDB Tables

- `projects`
- `scenes`
- `projectBibles`
- `promptPackages`
- `exportJobs`

The first schema version lives in `lib/db/schema.ts` and is registered through Dexie in `lib/db/index.ts`.
