import { PlaceholderPanel } from "@/components/shared/PlaceholderPanel";

export default function PromptsPage() {
  return (
    <PlaceholderPanel
      title="Prompts"
      description="Generate hero image prompts and Flow animation prompts from project bible and scene data."
    />
  );
}
