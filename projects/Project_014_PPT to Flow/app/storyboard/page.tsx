import { PlaceholderPanel } from "@/components/shared/PlaceholderPanel";

export default function StoryboardPage() {
  return (
    <PlaceholderPanel
      title="Storyboard"
      description="Break the project into numbered beats, scene intent, emotional arc, and timing."
    />
  );
}
