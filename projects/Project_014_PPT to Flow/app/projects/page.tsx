import { PlaceholderPanel } from "@/components/shared/PlaceholderPanel";

export default function ProjectsPage() {
  return (
    <PlaceholderPanel
      title="Projects"
      description="Create and manage Flow production projects, imported decks, target duration, and review status."
    />
  );
}
