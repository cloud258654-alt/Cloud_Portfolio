import { PlaceholderPanel } from "@/components/shared/PlaceholderPanel";

export default function ExportPage() {
  return (
    <PlaceholderPanel
      title="Export"
      description="Export production assets as Markdown, JSON, SRT, and Flow-ready prompt packages."
    />
  );
}
