import { PlaceholderPanel } from "@/components/shared/PlaceholderPanel";

export default function ScenesPage() {
  return (
    <PlaceholderPanel
      title="Scenes"
      description="Build scene cards with goals, camera, sound, transitions, ending frames, and QA score."
    />
  );
}
