import { PlaceholderPanel } from "@/components/shared/PlaceholderPanel";

export default function CharactersPage() {
  return (
    <PlaceholderPanel
      title="Characters"
      description="Define recurring people, wardrobe, roles, expression ranges, and continuity anchors."
    />
  );
}
